# Kinda 引き継ぎ — 2026-06-24（Code/SEO/計測 完了 → コラム・IG・note へ）

> claude.ai に渡す用の1枚サマリ。Code/SEO/計測の現状を最小限で共有する。

## 今日 Code 側で完了（すべて kinda.jp 本番反映済み）
1. **IG bio 着地**：`/note` → `/kinda-note`（307・UTM保持）。bio URL =
   `https://kinda.jp/note?utm_source=instagram&utm_medium=bio&utm_campaign=launch` が診断入口に着地。
2. **GA4 計測**：診断完了の実イベントは **`kinda_note_complete`**（※過去文書の
   `note_diagnosis_complete` は誤り）。キーイベント化済＝IG流入→診断完了の転換率が見える。
3. **SEO技術修正（本番反映）**：
   - 天気ページ(`/note/weather/[slug]`・一覧)に og:image / twitter:image 補完＝シェア画像が出る
   - `/kinda-note`・`/kinda-note/quiz` に canonical＋固有 title/description
   - sitemap に `/kinda-note` 追加
4. **www統一**：`www.kinda.jp` → `kinda.jp`（308）。評価分散を解消。

## 実機監査で判明した「正しい現状」（過去診断の思い込みを補正）
- **コラム `/columns/[slug]` は OG画像も構造化データも完備**：`opengraph-image.tsx` で動的OG画像、
  Article+Person+Organization+FAQPage の JSON-LD あり＝コラムは既にSEOが強い。
- **構造化データは過不足なし**：トップ=WebSite+SearchAction+Organization／columns=Article+FAQ／
  天気=BreadcrumbList／about=Organization。天気ページに Article は付けない（概念ページで型不適合）。
- **`/kinda-note`(気持ち整理) と `/kinda-type`(カウンセラー相性チェック＝送客) は別機能**。混同しない。

## サイトURL構造（コラムの内部リンク用・正）
- 診断入口：`/kinda-note` → `/kinda-note/quiz` → `/kinda-note/result`
- 天気：`/note/weather`(一覧) → `/note/weather/[slug]`(20種・うちコラム紐付き5本のみ index、残り15はnoindex)
- コラム：`/columns`(一覧) → `/columns/[slug]`
- 送客：`/kinda-talk`・`/kinda-talk/area/[area]`・`/kinda-talk/type/[type]`
- 内部リンク設計：IG/コラム(婚活KW可) → `/kinda-note`(中立) → `/note/weather` → `/columns` → `/kinda-talk`

## 残タスク
- **GSC 手動インデックス登録**（ふうか）：トップ→`/note/weather`・`/columns`→需要大コラム→主要天気の順。
- **IGキャプKW ↔ サイトSEO KW の統一**（未着手）：主表記＝結婚相談所/カウンセラー/婚活・婚活疲れ
  （コラム・IGキャプ用）、中立側(`/kinda-note`・天気)は 気持ち/天気/整理。

## コラム制作の前提（憲法）
- 既存コラム23本(`content/columns/*.mdx`)がフォーマット見本。frontmatter
  (title/description/category/author/publishedAt/weatherKey/thumbnail/tags)に従う。
- weatherKey は20種固定から選ぶ（新規作成禁止）。
- トーン：煽らない・比較しない・成婚をゴールにしない。著者＝さき/ふうか。

## claude.ai に渡す資料（最小推奨）
- このファイル（1枚サマリ）
- `docs/guides/weather-columns-writing-guide.md`（コラム執筆ガイド）
- サンプルコラム1本（例：`content/columns/good-counselor-traits.mdx`）＋ `src/app/kinda-note/data/weatherDescriptions.ts`（slug一覧）
- ※CLAUDE.md（憲法）は claude.ai 側に既知なら再投入不要（コンテキスト節約）。
